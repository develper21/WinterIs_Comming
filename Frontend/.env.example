# =================================
# SEBN FRONTEND ENVIRONMENT VARIABLES
# =================================

# API Configuration
VITE_API_BASE_URL=http://localhost:5000/api
VITE_API_TIMEOUT=10000

# Application Configuration
VITE_APP_NAME=SEBN Blood Bank Management
VITE_APP_VERSION=1.0.0
VITE_APP_DESCRIPTION=Save Blood Bank Network

# Development Configuration
VITE_DEV_MODE=true
VITE_LOG_LEVEL=info

# Authentication Configuration
VITE_TOKEN_STORAGE=localStorage
VITE_AUTO_REFRESH_TOKEN=true
VITE_SESSION_TIMEOUT=3600000

# UI Configuration
VITE_DEFAULT_LANGUAGE=en
VITE_THEME=light
VITE_PAGE_SIZE=20

# Map Configuration (Optional)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key
VITE_DEFAULT_LATITUDE=28.6139
VITE_DEFAULT_LONGITUDE=77.2090

# File Upload Configuration
VITE_MAX_FILE_SIZE=5242880
VITE_ALLOWED_FILE_TYPES=jpg,jpeg,png,pdf

# Feature Flags
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_NOTIFICATIONS=true
VITE_ENABLE_DARK_MODE=true
VITE_ENABLE_OFFLINE_MODE=false

# External Services (Optional)
VITE_SENTRY_DSN=your_sentry_dsn
VITE_GOOGLE_ANALYTICS_ID=your_ga_id

# Social Login (Optional)
VITE_GOOGLE_CLIENT_ID=your_google_client_id
VITE_FACEBOOK_APP_ID=your_facebook_app_id

# WebSocket Configuration (Optional)
VITE_WS_URL=ws://localhost:5000
VITE_ENABLE_REAL_TIME=true

# Cache Configuration
VITE_CACHE_DURATION=300000
VITE_ENABLE_CACHE=true

# =================================
# NOTES:
# =================================
# 1. Copy this file to .env and update with your actual values
# 2. Never commit .env file to version control
# 3. All VITE_ variables are accessible in your code via import.meta.env
# 4. Update VITE_API_BASE_URL for production deployment
# 5. Add your actual API keys for external services
# 6. Configure feature flags based on your requirements

# =================================
# DEVELOPMENT SETUP:
# =================================
# 1. Install dependencies: npm install
# 2. Copy .env.example to .env: cp .env.example .env
# 3. Update .env with your configuration
# 4. Start development server: npm run dev
# 5. Access app at: http://localhost:5173

# =================================
# PRODUCTION DEPLOYMENT:
# =================================
# 1. Set VITE_DEV_MODE=false
# 2. Update VITE_API_BASE_URL to your production API
# 3. Add your actual API keys and secrets
# 4. Build the app: npm run build
# 5. Deploy the dist/ folder to your hosting service